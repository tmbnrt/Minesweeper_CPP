#ifndef Minesweeper_CPP_H
#define Minesweeper_CPP_H

#include <iostream>
#include <string>
#include <vector>
#include <cstdlib>
//#include <ctime>
#include <SDL.h>

/*
struct Cell {
    bool visible;
    bool isMine;
    bool isFlagged;
    bool isDefused;
    int neighbourMines;
};

std::vector<std::vector<Cell>> board;

void initializeBoard(std::vector<std::vector<Cell>>& board);
void test_PrintBoard(std::vector<std::vector<Cell>> board);
int checkNeighbours(std::vector<std::vector<Cell>> board, int x, int y);
void countNeighbourMines(std::vector<std::vector<Cell>>& board);
*/

#endif // !Minesweeper_CPP_H

